#include <iostream>

using namespace std;

int main() {
    // instead of using float(3) / float(2) 
    // use 3. / 2.
    // float/integer = float
    float rasio = 3. / 2;
    printf("%.1f", rasio);
}